<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gfz.mvp">
    <uses-permission android:name="android.permission.INTERNET"/>

    <!--悬浮窗-->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.SYSTEM_OVERLAY_WINDOW"/>

    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".data.KTApp"
        android:theme="@style/AppTheme"
        android:allowBackup="false">
        <meta-data
            android:name="design_width_in_dp"
            android:value="375"/>
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.TestCalendarActivity" />
        <activity android:name=".ui.activity.TestMultiChooseActivity" />
        <activity android:name=".ui.activity.TestCountDownActivity" />
        <activity android:name=".ui.activity.TestClockActivity" />
        <activity android:name=".ui.activity.TestMoveActivity" />
        <activity android:name=".ui.activity.TestAnimationActivity" />

        <service android:name=".service.DrawOverService" />
    </application>

</manifest>